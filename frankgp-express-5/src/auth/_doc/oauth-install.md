# Accede a: http://localhost:3000/api/auth/google

```sh
npm install passport passport-google-oauth20 express-session
npm install --save-dev @types/express-session

npm i --save-dev @types/passport
```
